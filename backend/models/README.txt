Place your trained model file here:

  efficientnet_best.pt

This file is ignored by git (see .gitignore) because of its large size.

For deployment on Render.com:
  - Upload the file manually via the Render dashboard → Disks
  - Or use the setup_model.py script with your Google Drive link.
